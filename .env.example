# Base URL of the block-group boundary archives (PMTiles on Cloudflare R2, ADR-0002).
# Unset, the map reads /tiles from static/, which scripts/build-tiles.sh fills for local work.
PUBLIC_TILES_URL=https://pub-47753b768aae42b1ac3d4d90848098a1.r2.dev
