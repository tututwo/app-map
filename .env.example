# Production uses the same-origin /map-assets route backed by the Cloudflare R2 binding.
# Set PUBLIC_TILES_URL=/tiles locally to use archives built into static/tiles instead.
PUBLIC_TILES_URL=/map-assets
