# Required by the Cloudflare deployment and type-generation commands; keep this file tracked.
# Public configuration only. Store secrets in .env or Cloudflare, never here.
PUBLIC_TILES_URL=/map-assets
